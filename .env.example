# MongoDB Atlas connection (required for the API):
#   MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/delight_cafe
#   MONGODB_DB=delight_cafe
#   SESSION_SECRET=any-long-random-string
# Keep real values in .env or your hosting provider's secret settings.
