# Server
PORT=5000
NODE_ENV=development

# Optional MongoDB — leave empty to use in-memory data
MONGODB_URI=
MONGODB_DB=creative_studio

# CORS origin for the Vite dev server
CLIENT_ORIGIN=http://localhost:5173
