{
  "name": "Delight-Cafe",
  "version": "1.0.0",
  "description": "Delight Cafe website with Express backend and React frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "dev:react": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "build:react": "npm --prefix client run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "mongodb": "^6.18.0",
    "serverless-http": "^3.2.0"
  }
}
